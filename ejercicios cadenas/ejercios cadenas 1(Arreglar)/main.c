#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int validacadena(char[], int);
 /*cantidadnombres(char[][20]);*/
void mostrarnombres char[];
int main()
{
    char mat[10][20];

    char aux [50];

    cantidadnombres(nombre[]cantidad[20])

        while(validacadena(aux, 20)==0)
        {
            printf("Nombre no valido, Reingrese");
            gets(aux);
        }
        strcpy(mat[1],aux);
    for(int i=0;i<5;i++)
    {
        printf("%s\n",mat[i]);

    }

}

validacadena(char mat[20], int aux)
{
    esvalido = 0;

    if(strlen(mat)<aux)
    {
        esvalido=1;
    }

}
mostrarnombres(char nombres[][20], int cantidad)
{
    for(int i;i=cantidad;i++)
    {
        mostrarnombre(nombres[i]);
    }
}
