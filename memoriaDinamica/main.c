#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*int* vec;
    int num;
    int* aux;
    vec =(int*)malloc(5*sizeof(int));
    if(vec==NULL)
    {
        printf("No se enmcontro espacio en memoria");
        exit(1);
    }
    for(int i=0;i<5;i++)
    {
        printf("Ingrese un numero\n");
        scanf("%d",&num);
    }
    for(int i=0;i<5;i++)
    {
        printf("%d",*(vec+i));
    }
    printf("\n");
    free(vec);
    return 0;
    aux=(int*)realloc(*vec,10*sizeof(int));

    if(aux!=NULL)
    {
        vec=aux;
    }
    for(int i=0;i<10;i++);
    {
        printf("%d",*(vec+i));
    }
    printf("\n");
    free(vec);*/
    int cant;
    char* nombre;
    char auxcant[50];
    printf("ingrese nombre: ");
    cant=(auxcant);
    cant=strlen(auxcant);


}
